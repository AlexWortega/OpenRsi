- gen1 v0 [architecture] critique=6.7 SURVIVED: Keep the champion's winning recipe (residual target, circular lon padding, area-weighted normalized 
- gen1 v1 [target & normalization] critique=7.7 SURVIVED: Refocus the scaffold on the target and normalization pipeline: standardize the RESIDUAL by its own p
- gen1 v2 [loss design] critique=pruned: Keep the champion's residual-prediction + full-budget + circular-padding recipe, but sharpen the sin
- gen1 v3 [training schedule] critique=pruned: Keeps the champion's winning ingredients (residual target, circular lon padding, cos-lat weighted lo
- gen2 v0 [target & normalization] critique=7.0 SURVIVED: Keep the champion's proven residual+r_std+shrinkage core exactly, but make the two highest-leverage 
- gen2 v1 [loss design] critique=6.3 SURVIVED: Keep the champion's winning residual+shrinkage pipeline intact and target THIS variant's angle: the 
- gen2 v2 [training schedule] critique=pruned: The champion already nails residual-target normalization and shrinkage (skill 0.373). The untapped l
- gen2 v3 [augmentation & regularization] critique=pruned: Keep the champion's winning residual/standardization/alpha-shrinkage core intact, and add the varian
- gen3 v0 [loss design] critique=pruned: Keep the champion's proven residual + own-std standardization + shrinkage pipeline unchanged, and sh
- gen3 v1 [training schedule] critique=6.7 SURVIVED: Keep the champion's validated residual + own-std normalization + cos-lat loss + alpha shrinkage pipe
- gen3 v2 [augmentation & regularization] critique=7.0 SURVIVED: Keep the champion's winning residual+per-channel-std+shrinkage pipeline and cos(lat)-weighted loss e
- gen3 v3 [compute-budget usage] critique=pruned: Keep the champion's winning residual+own-std standardization+cos-lat-weighted loss+alpha shrinkage p
- gen4 v0 [training schedule] critique=6.0 SURVIVED: Keep the champion's proven residual + per-channel r_std standardization + cos(lat)-weighted loss + a
- gen4 v1 [augmentation & regularization] critique=pruned: Keep the champion's proven residual+per-channel-std+shrinkage+cos(lat)-loss pipeline exactly, and ad
- gen4 v2 [compute-budget usage] critique=pruned: Keep the champion's proven residual + per-channel r_std standardization + cos(lat)-weighted loss + a
- gen4 v3 [architecture] critique=6.0 SURVIVED: Keep the champion's winning residual + per-channel r_std standardization + cos(lat)-weighted loss + 
- gen5 v0 [augmentation & regularization] critique=pruned: Keep the champion's proven residual + per-channel r_std standardization + cos(lat)-weighted loss + a
- gen5 v1 [compute-budget usage] critique=5.7 SURVIVED: Keep the champion's proven residual + per-channel r_std standardization + cos(lat)-weighted loss + a
- gen5 v2 [architecture] critique=pruned: Keep the champion's proven residual + per-channel r_std standardization + cos(lat)-weighted loss + a
- gen5 v3 [target & normalization] critique=pruned: Keep the champion's proven residual + per-channel r_std standardization + cos(lat)-weighted loss pip
- gen6 v0 [compute-budget usage] critique=pruned: Keep the champion's proven residual + per-channel r_std standardization + cos(lat)-weighted loss + a
- gen6 v1 [architecture] critique=pruned: Keep the champion's proven residual + per-channel r_std standardization + cos(lat)-weighted loss + a
- gen6 v2 [target & normalization] critique=6.0 SURVIVED: Stay on the champion's residual+shrinkage pipeline but sharpen exactly the normalization angle: repl
- gen6 v3 [loss design] critique=pruned: Keep the champion's proven residual + per-channel r_std standardization + alpha shrinkage pipeline E
- gen7 v0 [architecture] critique=pruned: Keep the champion's proven residual + per-(channel,latitude) r_std standardization + cos(lat)-weight
- gen7 v1 [target & normalization] critique=6.0 SURVIVED: Keep the champion's residual + per-(channel,lat) std + shrinkage + cos(lat) loss pipeline exactly, a
- gen7 v2 [loss design] critique=pruned: Keep the champion's proven residual + per-(channel,lat) r_std standardization + per-channel alpha sh
- gen7 v3 [training schedule] critique=pruned: Keep the champion's exact residual + per-(channel,latitude) r_std standardization + cos(lat)-weighte
- gen8 v0 [target & normalization] critique=pruned: Sharpen only the target/normalization/output-scaling angle on top of the champion's proven residual+
- gen8 v1 [loss design] critique=pruned: Keep the champion's proven residual + per-(channel,lat) std + per-channel shrinkage + cos(lat)-weigh
- gen8 v2 [training schedule] critique=4.7 SURVIVED: Keep the champion residual plus per-channel-lat r_std plus per-channel trainable alpha shrinkage plu
- gen8 v3 [augmentation & regularization] critique=pruned: Keep the champion's winning residual + per-(channel,lat) r_std standardization + cos(lat)-weighted l
- gen9 v0 [loss design] critique=pruned: Keep the champion's proven residual + per-(channel,lat) r_std standardization + per-channel trainabl
- gen9 v1 [training schedule] critique=pruned: Keep the champion's proven residual + per-(channel,lat) r_std standardization + per-channel trainabl
- gen9 v2 [augmentation & regularization] critique=4.0 SURVIVED: Keep the champion's winning residual + per-(channel,latitude) r_std standardization + per-channel tr
- gen9 v3 [compute-budget usage] critique=pruned: Keep the champion's proven residual + per-(channel,latitude) r_std standardization + per-channel tra
- gen10 v0 [training schedule] critique=pruned: Keep the champion's proven residual + per-(channel,latitude) r_std standardization + per-channel tra
- gen10 v1 [augmentation & regularization] critique=pruned: Keep the champion's proven residual + per-(channel,latitude) r_std standardization + per-channel tra
- gen10 v2 [compute-budget usage] critique=pruned: Keep the champion's proven residual + per-(channel,latitude) r_std standardization + per-channel tra
- gen10 v3 [architecture] critique=4.3 SURVIVED: Keep the champion's winning residual + per-(channel,latitude) r_std standardization + per-channel tr
