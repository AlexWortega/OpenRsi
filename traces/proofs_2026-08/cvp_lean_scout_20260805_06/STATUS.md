# Status

**OPEN — no hardness lemma established.**

Generation 1 produced one exact finite counterexample. In the tested RS-amplified affine clause gadget with 64 evaluations, integer slack directions annihilate the full residual block. The exact squared objective is 27 against Boolean baseline 19 (distance ratio `sqrt(27/19)`), rather than acquiring residual squared cost at least 57. Residual-only RS amplification is killed in this form; algebraic-number and multi-prime variants with the same unamplified slack mechanism inherit the same causal defect.

Verified by `python3 experiments/verify_rs_slack_cheat.py` (exit 0).

Generation 2 leaves one bounded finite survivor: among all tested degree-one left-regular 12-column matrices, 18 of the 531,441 three-row matrices isolate the signed-selector fiber in `[-3,3]`; no one- or two-row matrix does. Verified by `python3 experiments/verify_affine_isolation_core.py` (exit 0).

Generation 3 removes the box caveat for exactly those 18 matrices. Integral left-kernel certificates and exact rational elimination prove all 126 associated harmful affine fibers empty over `Q`, hence over `Z`. Verified by `python3 experiments/verify_affine_isolation_unbounded.py` (exit 0).

Generation 4 produced no implementation result. Generation 5 kills private-row composition of the local survivor: all 5,832 tested ordered two-clause overlap systems have a nonzero integer kernel move of squared norm at most 4, covering 95,256 compatible references. The local certificate depends on fixing all three marginals and does not survive freeing unshared marginals. Verified by `python3 experiments/verify_overlap_composition.py` (exit 0).

Generation 6's constrained quotient output was invalidated at gate: normalization and consistency were external filters, the audit changed references, and the nine-clause instance has an unrestricted mod-2 bypass. Its finite SNF calculation is not CVP soundness evidence.

Generation 7 kills the selected multi-order radix mutation on that nine-clause instance. The emitted 154-dimensional fixed-target lattice contains all 72 anchors, 41 raw checks, and 41 cyclic base-33 residual rows. Its exact unrestricted squared minimum is 80 versus Boolean completeness squared radius 72. The minimizer is a zero-residual signed selector (`011 + 100 - 111` in the sole clause falsified by `0000`), so every radix coordinate vanishes. Verified by `python3 experiments/verify_multiorder_radix_barrier.py` (exit 0). This is a finite counterexample to the tested mutation only.

Generation 9 gives a finite pass for the sole authorized survivor, a two-parameter incidence-equivariant global PSD metric. Exact rational factor/Gram/center data are emitted for both the obstruction and a satisfiable overlapping control. Their exact unrestricted squared minima are 96 and 72, so the finite distance ratio is `sqrt(4/3)>1.1`. The unsatisfiable nearest vector is nevertheless the seven-term degree-two cube-parity kernel, at constant anchor excess 24. Verified by `python3 experiments/verify_global_psd_metric.py` (exit 0).

Generation 11 kills the authorized fixed cubic extension on the same instances. Cubic rows charge the inherited clause-3 parity, but global triple `(0,2,3)` has only one clause occurrence, allowing the same seven-term parity in clause 1 with zero residual and anchor excess 24. Exact unrestricted squared minima remain 96 and 72. Verified by `python3 experiments/verify_degree3_global_psd_metric.py` (exit 0).

Generation 12 kills one explicit surviving spherical-fingerprint candidate. A dimension-9 clausewise top-Walsh tag raises cube parity to squared distance 153 and gives honest squared radius 81, but an unrestricted clause-drop vector has exact squared distance 105, below the prescribed `4/3` threshold 108. The control exact minimum is 81. Verified by `python3 experiments/verify_spherical_parity_fingerprint.py` (exit 0). Other global Gram/tag families remain untested.

Generation 13 kills the bounded raw-selector linear-code mutation at its required compatibility audit. Over `p=2,3,5,127`, honest differences have rank 14 and maximal compatible syndrome dimension 58. The Generation-11 anchor-excess-24 parity is an integral affine combination of honest encodings with coefficients summing to one, so every compatible linear syndrome is exactly zero on it. Verified by `python3 experiments/verify_selector_code_compatibility.py` (exit 0). Nonlinear and enlarged encodings are not covered.

Generation 14 gives a finite shell pass for the sole surviving complete pair-bag lift. Its 520 selectors and 612 emitted rows have no obstruction vector through squared radius `B+32=552`; the satisfiable control exact minimum is `B=520`. The G11 affine collision lifts only at squared distance 1448, while reconstructed G7 and drop attacks fail the audited shell. Verified by `python3 experiments/verify_pair_bag_lift.py` (exit 0). No fixed-level composition or growing-gap result follows.

Generation 15 kills one preregistered weighted sparse laminar hierarchy. The 200-selector hierarchy has exact control minimum `B=18560` and proposed threshold `T=25472`, but the G13 affine coefficients lift the G11 parity through every level with zero residual at squared distance 24960. Verified by `python3 experiments/verify_weighted_laminar_hierarchy.py` (exit 0). This does not rule out other sparse hierarchies or weight rules.

Generation 19 kills the sole surviving width-5 Barrington flow encoding. The fully emitted 22,754-variable, 17,555-row instance has a zero-residual accepting signed flow with two `-1` coefficients and anchor excess 16. Exact obstruction and control squared minima are 22,770 and 22,754. Verified by `python3 experiments/verify_barrington_signed_flow.py` (exit 0). No signed-flow lower bound or growing gap follows.

Generation 28 exhausts the sole authorized frozen reduced pair-tile recursion through squared radius 57. The rank-32 obstruction/control exact squared minima are 57 and 32. Complete depth-one tables have adverse and legal minima 32 and 16, so the tested growth factors are `lambda=57/32<mu=2`. The emitted partition includes DROP, G13, G19, and all remaining malformed ports. Verified by `python3 experiments/verify_frozen_minplus_pair_tile.py` (exit 0). This finite rule fails its required growth inequality.

Generation 30 kills the sole authorized literal-tensor test on the serialized G28 depth-one seed pair. A coordinate swap `0<->7` maps the alleged NO factor and target exactly to the control; its tensor square pairs every unrestricted integer coefficient matrix. Thus `R_1=R_2=1`, rather than `R_2>R_1`. The complete named-attack shell is paired algebraically, and 131,585 matrices with `l1<=2` are searched exactly. Verified by `python3 experiments/verify_literal_tensor_seed_isometry.py` (exit 0). This is finite evidence about this seed pair only.

Generation 31 gives a finite strict-four-thirds shell pass for the sole authorized equal-radius Gram. The exact control squared minimum is 144. Exhaustive zero-residual moment DP and residual-one low-base enumeration prove no obstruction vector through 192; a parity witness gives upper bound 216. Thus the finite obstruction minimum lies in `[193,216]`, with certified squared ratio at least `193/144>4/3`. Verified by `python3 experiments/verify_equal_radius_walsh_gram.py` (exit 0). No composition or dimension-dependent growth follows.

Generation 32 kills the selected cross-copy composition rule. Exact DP determines the one-copy obstruction minimum `d_1^2=216`, and residual-branch accounting gives two-copy control minimum 288. Despite 125 cross-copy degree-at-most-three moment rows, two compatible parity witnesses have zero residual and cost `432=2d_1^2`; strict superadditivity fails. Verified by `python3 experiments/verify_crosscopy_walsh_composition.py` (exit 0). This is finite evidence about this coupling only.

Generation 33 rejects the sole selectable exterior-bivector mutation at its preregistered completeness gate. For all 512 clause-incidence sign rules, the 16 honest control points have common-sphere system rank 4 and augmented rank 5; the obstruction has the same failure. No rational center exists, so no factor/target or soundness shell is authorized. Verified by `python3 experiments/verify_exterior_bivector_completeness.py` (exit 0). This kills only the explicit Vandermonde-bivector family.

Generation 34 rejects the authorized positive-definite repair of those tags. Across all 512 sign rules, exact center-eliminated equal-sphere constraints have one common rank-10 RREF containing `G[1,1]=0`. This contradicts the preregistered `G>=I/100`, which requires `G[1,1]>=1/100`. Verified by `python3 experiments/verify_exterior_metric_repair_infeasible.py` (exit 0). No factor or shell follows from this finite infeasibility result.

Generation 37 kills the authorized two-level incidence-orbit metric family. Under every normalized anchor/Walsh weighting, the fixed one-copy parity costs `96alpha+120beta`, while two compatible copies cost exactly twice that and annihilate all cross residuals. The exact cut is `delta<=0`; at the emitted rational metric the costs are `3/2` and 3, with control minima 1 and 2. Verified by `python3 experiments/verify_twolevel_metric_parity_cut.py` (exit 0). This does not optimize arbitrary Gram families.

Generation 38 gives a finite `B+64` pass for the authorized 12 splitter bags. The obstruction rank is 117 and no unrestricted vector exists through 181; the matched control exact minimum is 119. Eleven full-variable bags force any residual-square-at-most-two state to one common distribution, but their common legal support is empty and normalization then fails in 11 rows. Verified by `python3 experiments/verify_splitter_clause_bags.py` (exit 0). No bag-family scaling or growing ratio is established.

Goal-directed Generation 1 gives a finite counterexample to the explicit complete linear ordered-pair realization of Strategy 1. The hash-locked G19 two-negative flow embeds diagonally in the rank-224,282 `k=2` lift, satisfies all 348,451 emitted rows, and costs 224,330 against exact control squared radius 224,282. Thus it is below both the weaker `4R_2^2/3` test and the displayed FRONTIER value `16R_2^2/9`. The pure tensor itself fails 14 diagonal rows, but diagonal coherence does not remove the signed splice. Verified by `python3 experiments/verify_k2_barrington_tensor_splice.py` (exit 0). This is finite evidence about the frozen lift, not an exact `k=2` optimum or an asymptotic theorem.

Goal-directed Generation 2 kills the sole authorized `A5` convolution tile. A depth-two rank-10,800 NO instance with all four leaves fixed to identity has a zero-residual bicyclic-unit fusion to the G19 ACCEPT cycle at squared cost `10936a^2`; a matched honest control has exact radius squared `10800a^2`. The exact inequality `32*10936 < 33*10800` rejects all 144 factors `C=[aI;bA]`, `1<=a,b<=12`, independently of `b`. Verified by `python3 experiments/verify_a5_bicyclic_fusion_attack.py` (exit 0). This is a finite kill of the frozen tile, not an asymptotic result.

Goal-directed Generation 3 rejects the sole repaired `D4` triality family at the exact Voronoi-completeness gate. Across 2,924,544 positive-definite Gram/label candidates, legal COPY antipodes have the allowed malformed midpoint zero strictly inside their common sphere; legal NAND states `011,101` have the allowed midpoint `(0,0,output-1)` strictly inside. Minimum exact inward deficits are `3/4` and `17/4`. Verified by `python3 experiments/verify_d4_triality_midpoint_obstruction.py` (exit 0). No factor or transfer table follows, and no general Voronoi-tile impossibility is claimed.

Goal-directed Generation 4 rejects the complete prescribed non-antipodal `D4` grid at the NAND sphere gate. Exact signature compression covers 631,701,504 label/Gram candidates. Equal legal NAND radius forces at least one false Boolean port on or inside the shell in every case: 528,417,792 strict interiors and 103,283,712 ties. Verified by `python3 experiments/verify_nonantipodal_d4_nand_obstruction.py` (exit 0). COPY/factor/transfer construction is not authorized, and no general Voronoi impossibility is claimed.

Goal-directed Generation 5 rejects the frozen independent-coupling `D4` grid at the global NAND-shell gate. Of 2,239,488,000 candidates, 24,344,064 pass strict false-Boolean separation, but every survivor has a malformed coordinate-recombination point on or inside the legal radius by an exact two-hybrid energy identity. Verified by `python3 experiments/verify_independent_d4_recombination_obstruction.py` (exit 0). COPY and transfer construction do not follow; nonseparable coordinate Grams remain untested.

Goal-directed Generation 6 rejects the prescribed `E6` Gosset port-map family. The verifier gives an exact empty-sphere certificate for all 27 shell vertices at radius squared `4/3`, then covers all 387,420,489 bounded integral `3x6` maps via exhaustive row classification. Only the zero row is binary-valued on the shell, so no map sends every vertex to the four legal NAND words while hitting all four. Verified by `python3 experiments/verify_e6_gosset_port_map_obstruction.py` (exit 0). COPY and transfer construction do not follow; larger maps and other Delaunay cells remain open.

Goal-directed Generation 7 completely classifies rational affine binary-valued rows on the fixed 27-point `E6` shell. Exact interpolation of all 128 assignments on a seven-vertex affine basis leaves only the constant rows 0 and 1; no retained triple realizes any translated/relabelled NAND relation. Verified by `python3 experiments/verify_e6_unbounded_affine_port_no_go.py` (exit 0). This closes affine ports for this finite shell only; nonlinear/redundant ports, COPY, and transfer growth remain open.

Goal-directed Generation 8 rejects the sole repaired augmented-Gram family before enumeration. With legal squared energy 64 and every extended-Gram entry bounded by 64, the unrestricted zero selector has false NAND port `000` and energy `s<=64`, while `65/64` soundness requires at least 65. Verified by `python3 experiments/verify_augmented_gram_drop_obstruction.py` (exit 0). This exact DROP contradiction applies only to the frozen normalization; rescaled augmented families remain untested.

Goal-directed Generation 11 kills the canonical eight-selector `F_289` NAND template at grade zero. Every false NAND boundary has a unique integral legal-selector pseudosection; three have zero residual and anchor energy 16. They are Frobenius-fixed, remain grade zero, and cost less than the minimum nonzero prime-ideal trace energy 34. Verified by `python3 experiments/verify_f289_nand_affine_grade_zero_attack.py` (exit 0). No maximal-order lift, COPY module, or depth-two audit follows; other quaternion modules remain open.

Goal-directed Generation 12 leaves a finite depth-one NAND survivor. Exhaustion of all 490,314 `N=8` signature multisets finds 13,457 saturated local passes. The best determinant-one module has legal energy 8 and exact false energies `160,64,56,56`. An explicit maximal order and two-sided prime computation independently certifies minimum nonzero trace energy 34, so the local minimum 56 passes both required gates. Verified by `python3 experiments/verify_redundant_signature_nand_survivor.py` (exit 0). No COPY module, depth-two growth, or all-depth theorem is established.

Current Generation 1 selects the cross-review-surviving mutated adverse-transducer route, but its explicit affine realization is finitely falsified. The natural determinant-one NAND residual map has exact grade-zero false fibers `010`, `100`, and `111` at squared energies 64, 56, and 56. All 378 saturated binary-signature affine COPY codes of ranks 2–8 admit a zero-residual `false111-COPY11-false111` splice in both orientations, with total energy 114–120. Verified by `python3 experiments/verify_redundant_nand_grade_zero_seed.py` and `python3 experiments/verify_quaternion_copy_diagonal_splice.py` (exit 0). This does not rule out a newly emitted quaternionic transfer coupling.

A reusable universal lemma is established in Lean: local integer potential inequalities telescope on every finite walk; bounded potentials give a uniform transient bound; and one valuation gain per four binary levels beats binary scaling at prime 17. Verified by compiling `lean/Verify_transducer_potential.lean` against Mathlib with no `sorry`, `admit`, or new axiom. The lemma is conditional and is not instantiated by the current NAND/COPY data.

Current Generation 3 tests the cross-review-surviving generic product-tag specialization at the required enlarged-kernel gate. In the frozen `4x2` seam, all six old rectangles and false `111` receive nonzero tags, but exact enumeration of `3^8=6561` vectors finds a conformally primitive signed move with zero margins and exact zero product tag, `l1=8` and squared coefficient weight 8. A distinct asymmetric `8x8` stress labeling also has a two-rectangle movement with coefficients `(2,-1)`, zero integer margins, zero `F_289` transfer, squared weight 12, and support 6. Verified by `python3 experiments/verify_product_tag_enlarged_kernel.py` and `python3 experiments/verify_product_tag_rectangle_kernel.py` (exit 0). These are seam-kernel results, not full-tile reachability or CVP-energy certificates.

Two universal local lemmas are established in Lean. `lean/Verify_product_tag_rectangle.lean` proves product-tag rectangle factorization and exact division-ring separation for distinct labels, including a skew-automorphism variant. `lean/Verify_three_transfer_kernel.lean` proves that every three `F_289` leading symbols, modeled as `F_17^2`, admit a nonzero coefficient combination with zero leading symbol. Both compile against Mathlib without `sorry`, `admit`, or added axioms. Together they show why individual rectangles can pass while their enlarged-kernel combinations fail.

Current Generation 4 performs the unanimously required fixed-witness lift-or-kill audit, but no intended maximal-order candidate was supplied. In the hash-locked canonical margin-only completion, each orientation has an 18-variable `22x18` emitted matrix and sixteen total `40x18` factor/target instances across both orientations. All legal fibers have squared energy `E=18`. Exact kernel and shell search finds a conformally primitive malformed exact-residual-zero vector of squared distance `42<306=17E` in all 16 legal cell/orientation fibers. A related independent `36x18` decoded-margin serialization gives the same result. Verified by `python3 experiments/verify_product_tag_full_lift_attack.py` and `python3 experiments/verify_product_tag_full_seam_lift.py` (exit 0). This finitely kills only those margin-only completions, not unspecified extra pair-dependent rows.

A universal conditional lift obstruction is established in Lean. If three integrally independent seam directions survive every emitted non-transfer integer row while their leading symbols lie in one `F_17^2` coordinate, then a nonzero integer combination survives those rows, has zero leading transfer, coefficients in `[-8,8]`, and squared coefficient weight at most 192. `lean/Verify_single_transfer_lift_obstruction.lean` compiles against Mathlib without `sorry`, `admit`, or added axioms. It does not prove survival in a missing full tile or a sub-`17E` CVP bound.

Current Generation 5 tests the sole cross-review-surviving multi-channel product mutation. A two-channel array has rank three modulo 17 on the frozen zero-margin pair seam, detects the Generation-4 witness, and has no zero-syndrome malformed state among 7,152 same-margin pair selectors below `17E`. Verified by `python3 experiments/verify_multichannel_product_shell.py` (exit 0). This restricted finite pass omits physical-selector deviations.

The breaker supplies the operative counterexample. For each channel prefix `r=1,2,3,4`, both orientations, and all legal cells, a one-bit physical NAND-selector flip leaves every pair-supported transfer row unchanged. Exact search checks 640 Hamming-one candidates across 64 fibers and finds minimum squared distance `20<306=17E`, with legal energy `E=18` and zero vector transfer in every fiber. Verified by `PYTHONPATH=experiments python3 experiments/verify_multichannel_physical_flip_attack.py` (exit 0). This finitely kills the four unscaled 18-variable serializations, not unspecified extra or scaled rows.

A universal conditional multi-channel lemma is established in Lean. Componentwise nonzero division-ring transport preserves and reflects zero vector syndrome; injective syndrome detects all nonzero defects; `r` residue channels have exact `F_17` finrank `2r`; injectivity forces defect finrank at most `2r`, and above capacity every linear syndrome has a nonzero kernel. `lean/Verify_multichannel_transfer.lean` compiles against Mathlib without `sorry`, `admit`, or added axioms. It constructs no tile and proves no energy, carry, or recursion statement.

Current Generation 6 tests marked oblivious Beneš routing, the only cross-review survivor at the new L1 frontier. The breaker freezes a `54x76` pair-linearized switch matrix and finds a conformally primitive support-eight toric exchange with exact zero residual. It equals `h000-h011-h100+h111`, lies in the honest affine-difference lattice, and yields malformed energy 92 or 108 versus legal energy 76 in all 384 permutation/input fibers. Verified by `python3 experiments/verify_benes_switch_toric_exchange.py` (exit 0). This finitely kills that brick, not all Beneš implementations.

A separate `136x142` target-only local serialization has equal legal energy 142 over 384 encodings, detects every embedded local primitive on setting-forbidden coordinates, and has no physical-only Hamming-one/two kernel move among 5,832 tests. Its frozen residual scale 5 is nevertheless killed by DROP: zero has squared distance `692<2414=17*142`. Verified by `python3 experiments/verify_marked_benes_local_audit.py` (exit 0). No depth-1/2/3 Lawrence identity follows.

A universal metric lemma is established in Lean. Any square integer matrix with Gram identity is a signed permutation matrix, so arbitrary unimodular column operations do not preserve Euclidean energy or selector support. `lean/Verify_integral_euclidean_isometry.lean` compiles against Mathlib without `sorry`, `admit`, or added axioms. It proves only the marking restriction, not L1 or soundness.

Current Generation 7 tests the sole opponent-review survivor, the equal-radius quadratic-character switch. The breaker exhausts the local `{-1,0,1}^4` box and finds three support-three normalized ghosts mapping to non-honest sign words of the same squared norm 4 as honest words. For each of 65 tested mark scales 0–64, normalization is raised minimally so DROP reaches at least `17E`, yet the ghost remains below threshold. Verified by `python3 experiments/verify_quadratic_character_ghost.py` (exit 0). This finitely kills the frozen local objective only.

A fuller hash-locked `74x44` three-COPY-cycle serialization is also killed. Exact search of all 531,441 signed COPY movements finds a conformally primitive synchronized support-12 rectangle. It preserves all 74 rows and yields malformed energy exactly `885`, equal to legal energy, in every honest fiber; DROP costs 120,000, above `17E=15,045`. Verified by `python3 experiments/verify_quadratic_character_copy_cycle.py` (exit 0). This finite result does not cover every augmented quadratic-character brick.

A universal local lemma is established in Lean. The four character words have common squared radius 4, mixed derivative `-2` in the quadratic coordinate, no nontrivial integer linear relation, and wire swap given by a marked coordinate permutation. `lean/Verify_quadratic_character_switch.lean` compiles against Mathlib without `sorry`, `admit`, or added axioms. It proves no complete brick, L1 identity, or soundness bound.

Current Generation 8 tests the sole cross-review-surviving separator/treewidth mechanism at the new U0 frontier. A hash-frozen affine-detector surrogate has exact top-level balanced-separator optima `4,6,9` for `S=8,16,32`, independently certified by HiGHS and CBC; 654,384 sparse signed defects also pass the declared detector-image test. Verified by `python3 experiments/verify_gen8_affine_detector_separator.py` (exit 0). This is finite surrogate evidence, not an actual universal-circuit serializer, hereditary profile, fixed-class bound, or U0.

The breaker supplies a real limitation on support-only reasoning. Synthetic cumulative controls with certified `K_{n/2,n/2}` support subgraphs at `n=8,16,32` simultaneously have support-two and support-three kernel moves and a unimodular row-equivalent tree-incidence presentation. Verified by `python3 experiments/verify_gen8_separator_kernel_degeneracy.py` (exit 0). This does not refute the different affine-detector surrogate, and unrestricted row rebasing is not automatically an admissible U0 operation; it shows that displayed support growth alone is neither signed soundness nor a basis-robust tractability exclusion.

A universal minor-plumbing lemma is established in Lean. The support graph of `C` is induced in and is a minor of the support graph of `[I|-C]`; identity columns are leaves; and every equality expansion with an onto collapse, connected fibers, and edge lifts supplies an explicit branch-set minor model. `lean/Verify_support_minor_channel.lean` compiles against Mathlib without `sorry`, `admit`, or added axioms. It proves no separator growth, treewidth lower bound, fixed-class upper bound, or U0.

No hardness lemma or dimension-dependent gap is established. Full-brick Lawrence is retired as the primary route. U0 remains open because no actual `C_S` universal-circuit family, precise admissible grammar, hereditary separator theorem, or separate closure result for the four fixed-template classes has been supplied.

Target: Prove a deterministic polynomial-time many-one reduction from 3SAT to Euclidean GapCVP with approximation factor n^c for an explicit absolute c>0, without PCP and without unproved conjectures.
