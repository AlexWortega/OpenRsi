Using only the supplied campaign files, the following audit key expands every obstruction named in `ROADMAP.md`. Every sketch checks all six groups explicitly.

- **A — emission failures:** G1 RS slack; G6 externally filtered quotient; G7 radix kernel.
- **B — signed/affine seams:** G2–3 local affine isolation; G5 private-row overlap; G9 degree-two parity; G11 unique-triple parity; G13 honest-affine-span collision; G15 laminar lift; G19 signed splicing; Goal G1 diagonal ordered-pair splice; fresh toric quadratic exchange.
- **C — DROP:** G12 fingerprint DROP; Goal G8 augmented-Gram DROP.
- **D — false amplification:** G14 pair bags; G28 \(\lambda\le\mu\); G31 Walsh Gram; G32 additive parity; G37 universal parity cut; G38 splitter bags.
- **E — closed families:** G30 seed isometry; G33–34 exterior completeness; Goals G3–5 \(D_4\); Goals G6–7 \(E_6\); Goal G2 \(A_5\) zero divisors.
- **F — quaternion frontier:** Goal G11 grade-zero attack; Goal G12 redundant NAND; killed affine COPY frontier; carry/lumpability.

## 1. Hash-locked lift-or-kill certificate for the current tag

**Mechanism.** Serialize the actual \(N=8\) NAND and both COPY orientations, then fix each sign of the known \(4\times2\) zero-tag seam vector and minimize over every omitted selector. This directly decides whether the present single-tag candidate already has a below-\(17E\) witness.

**Expected move.** Either refute Q1 with a full CVP witness, or eliminate precisely this witness by SNF/left-kernel certificates. Promotion requires a Lean theorem checking the concrete matrix equations, energy, coefficient bound, and exhaustive shell certificate.

**Audit.** A: every equation is emitted. B: not escaped—the complete signed lift is tested. C: zero selector and all dropped blocks are included. D: no amplification is inferred. E: no closed geometric/tensor family is used. F: uses the actual order, prime, \(P^2/P^3\) coordinates, and both COPY orientations rather than the commutative proxy.

**Falsification/death.** Most likely the witness lifts cheaply; alternatively, no coherent “actual candidate” matrix can be serialized.

**Smallest experiment.** Implement the gate-requested fixed-witness bounded minimization before computing any Graver basis.

## 2. Multi-coordinate quaternion parity-check code

**Mechanism.** Replace one \(F_{289}\) transfer coordinate by \(r\) independently labelled product coordinates. Choose labels so the resulting \(2r\times q\) \(F_{17}\)-matrix is injective on the surviving seam quotient of dimension \(q\), analogous to a parity-check code with prescribed rank-one rows.

**Expected move.** Defeat `Verify_three_transfer_kernel` by taking \(2r\ge q\), then recompute the enlarged Graver basis; Q1 follows only if every non-honest primitive remains nonzero and the shell conditions pass. Lean target: injective transfer implies nonvanishing for every listed quotient vector, over `ZMod 17`.

**Audit.** A: all \(r\) coordinates are emitted. B: full post-tag Graver recomputation handles every named splice. C: DROP is separately enumerated. D: only Q1 is claimed; Q2–Q3 remain required. E: unrelated families are unused. F: directly addresses grade-zero dimension, redundant NAND, COPY, and requires complete lift sections.

**Falsification/death.** Product-evaluation rows may span too small a subspace, or adding coordinates may create new Lawrence moves.

**Smallest experiment.** From the serialized seam, compute \(q\), then SAT-search \(r=\lceil q/2\rceil,\lceil q/2\rceil+1\) label systems modulo \(P\).

## 3. Saturated quotient with regular-simplex tags

**Mechanism.** Compute the saturated lattice \(H\) generated by honest seam transports inside \(K=\ker_{\mathbb Z}A\), and tag the quotient \(K/H\) by a full integral basis rather than quaternion products. Realize the basis as centered regular-simplex coordinates, so legal pair states have equal energy while every deviation outside \(H\) has nonzero transfer.

**Expected move.** Amend Q1 from product tags to a universal seam-quotient tag. Lean target: if \(H\) is saturated and \(B\) presents \(K/H\), then \(Bg=0\iff g\in H\), together with the common-radius simplex identity.

**Audit.** A: quotient rows are emitted, not filtered. B: G13 is not escaped—if a harmful state lies in \(H\), the mechanism fails; all other signed attacks are tested in \(K/H\). C: DROP gets its own quotient class and shell test. D: no growth claim without a replacement for Q2–Q3. E: no listed closed family. F: avoids finite-field dimension and carries, but honestly abandons graded-division transfer.

**Falsification/death.** The toric exchange or affine pseudodistribution may already belong to the saturated honest subgroup.

**Smallest experiment.** Compute SNF for the complete seam’s \(H\subset K\), then test whether every below-\(17E\) adverse vector has nonzero quotient coordinates.

## 4. Universal algebraic-geometry decision of product-tag feasibility

**Mechanism.** Treat all quaternion residue labels as variables through explicit \(F_{17}\) structure constants. For each primitive \(g\), impose \(N(T(g))\ne0\) using an inverse variable \(z_gN(T(g))=1\); Gröbner elimination or a finite-field Nullstellensatz certificate then decides whether any simultaneous specialization exists.

**Expected move.** Either synthesize exact labels for Q1 or formally refute the entire frozen number-of-tags/product-degree family—not merely another labeling. A Lean checker can verify the resulting polynomial identity or exact satisfying assignment.

**Audit.** A: emitted-matrix constraints enter the ideal. B: primitives are recomputed by CEGIS whenever a tag changes; otherwise the test is honestly incomplete. C: DROP and false fibers contribute equations. D: this decides only Q1 feasibility. E: no tensor, exterior, \(D_4/E_6\), or group-algebra assumptions. F: grade zero is exact; \(P^2/P^3\) and section stabilization remain separate mandatory ideals.

**Falsification/death.** The Graver basis depends on the specialization, producing an intractable CEGIS loop; a grade-zero solution may still fail every lift.

**Smallest experiment.** Run the universal calculation on the complete \(4\times2\) seam with two tags and only the certified low-energy primitives.

## 5. Finite nonabelian holonomy automaton

**Mechanism.** Replace additive product sums by a tabulated finite-state automaton whose transition labels are quaternion units; selectors encode \((\text{state},\text{input},\text{next state})\). Honest seams have prescribed endpoint holonomy, while a rectangular exchange should terminate in a different state.

**Expected move.** Produce a nonlinear-in-history but linearly emitted Q1 replacement. Beyond finite testing, Lean must prove from the complete transition table that every integral adverse flow either has nontrivial endpoint or costs at least \(17E\).

**Audit.** A: all flow, endpoint, and transition constraints are coordinates. B: G19 and Goal G1 are not escaped; complete signed-flow Graver enumeration is mandatory. C: zero flow and missing layers are explicit DROP states. D: no recursive gain follows until a Lean holonomy/potential theorem is instantiated. E: unlike Goal G2, this uses permutation transitions, not group-algebra convolution; other closed families are absent. F: finite exact states remove carries, but the redundant NAND and COPY seeds remain required.

**Falsification/death.** A signed accepting circulation analogous to G19 will probably erase the endpoint distinction.

**Smallest experiment.** Use \(Q_8\) or the smallest quaternion-unit quotient on the frozen \(4\times2\) seam and compute its complete signed-flow Graver basis.

## 6. Oriented-matroid cone certificate against cancellation

**Mechanism.** Orient every Graver move by its adverse boundary class and seek a rational functional \(\ell\) with \(\ell(T(g))\ge1\) for every oriented adverse primitive. Then every conformal sum remains nonzero, eliminating the present “individually nonzero but collectively cancelling” defect.

**Expected move.** Replace Q1’s weak pointwise nonvanishing by a pointed-cone condition. Lean target: conformal decomposition plus the inequalities implies \(\ell(T(x))\ge\) the number of adverse primitive summands, followed by the existing potential telescope.

**Audit.** A: \(T\) and \(\ell T\) are emitted. B: every named signed attack is included with its actual orientation; if both \(g\) and \(-g\) are adverse, the LP correctly fails. C: DROP receives oriented generators. D: unlike finite Gram passes, the proposed Lean cone theorem is compositional. E: no listed closed family. F: this amends away from \(P\)-adic division; grade-zero, COPY, and carry issues are replaced rather than ignored.

**Falsification/death.** Farkas will likely find a positive circuit of adverse moves summing to zero—the toric exchange is a natural seed.

**Smallest experiment.** Compute oriented primitives below \(17E\) for the complete seam and solve the exact rational separation LP, emitting either \(\ell\) or a Farkas circuit.

## 7. Joint Gram-and-potential synthesis

**Mechanism.** Abandon transfer symbols and simultaneously synthesize a rational positive-definite Gram matrix \(Q\), finite state partition, and potential \(h\) satisfying common legal energy and strict recursive inequalities for every unrestricted transition. This merges Q1 and Q3 so a bad local state may be tolerated only when its potential-adjusted cost grows.

**Expected move.** Amend the roadmap edge only if an exact finite certificate gives \(\lambda/\mu>1\); Lean must prove the shell tail bound and telescope the complete transition inequalities for arbitrary depth.

**Audit.** A: the objective contains every constraint. B: states arise from exhaustive integral fibers, not named attacks. C: zero and partial blocks are states. D: directly addresses G28 and the non-growth lessons G14/G31/G32/G37/G38 rather than extrapolating a finite gap. E: no isometric seed or closed geometric family. F: avoids the blocked single \(F_{289}\) coordinate, but requires an explicit replacement for Q2; redundant NAND/COPY are still inputs.

**Falsification/death.** The exact SDP/LP is likely infeasible because DROP or a parity splice forces \(\lambda\le\mu\), or the state partition fails to close.

**Smallest experiment.** Freeze the \(N=8\) NAND plus one COPY seam, enumerate the smallest provably complete shell, and solve rationalized Gram/potential inequalities with a Farkas certificate on failure.
