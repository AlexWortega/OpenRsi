# Consultation 10: global rows beyond affine tableaus/marginals

Respect the strict prohibition on the named recent document and every copy, mirror, summary, discussion, or recollection of it. Reason independently only from the attached failure map.

Goal remains a deterministic polynomial reduction from 3SAT to polynomial-gap binary NCP or Euclidean CVP without PCP. There is still zero ladder progress.

New failures since consultation 9:

1. Full-degree local 3-clause truth tables over the integers, coupled by unary marginals, have an exact signed parallelogram repair of support/squared norm 13 on the all-eight core.
2. Integer connected scopes have a scalable mixed-orbit holonomy cheat. Orbit sizes two and three, weighted -1 and +1, give mass one and exact marginals. This has an explicit bounded-occurrence exact-3CNF realization with 43n clauses, 27n variables, maximum occurrence 17, and witness squared norm at most 25K at every proper depth.
3. Integer all-pairs/full-intersection scopes also fail. Tagged inconsistent F2/F3 charged Petersen flows with fixed signed masses -80 and 81 give integral mass-one local tables and exact full-intersection marginals. Disjoint unions have constant support/group and squared-norm/group ratios tending to 97 and 481.
4. Consultation 9's bounded-fan-in circuit-tableau OR fault was fully implemented: any compact global determinant/resultant predicate evaluated through local truth tables and affine wire interfaces has an additive-cost-two accepting fault.
5. Global CRT assignment coupling does not help if clause validity remains an affine exact slack fiber.

Thus a surviving construction needs explicit **global assignment-dependent rows** that do not factor through bounded local/tableau/marginal interfaces. Give exactly ONE candidate or one sharp no-go theorem, not a survey.

Preferred candidate form: use dense global characters/moments of the entire assignment (for example Fourier characters, exterior powers/minors, or code-dependent dense tensor functionals) as matrix rows, but specify how their values are generated linearly from sparse witness columns. Required:

* exact matrix/basis, target, coefficient domain, dimensions, and polynomial-time construction;
* exact completeness cost;
* intended polynomial soundness gap against arbitrary signed/GF(2) coefficient combinations;
* explicit defense against mixed 2/3 virtual probability measures and the support-three circuit fault;
* smallest all-eight-core hostile search to implement;
* parameter accounting in final rank N;
* isolate every unproved lemma.

If no candidate survives, prove a sharp theorem that finite polynomially-many global moments/characters still admit a low-norm signed virtual measure on an UNSAT instance. State hypotheses tightly and give an implementable finite experiment. Do not return another local gadget or routine hierarchy.
