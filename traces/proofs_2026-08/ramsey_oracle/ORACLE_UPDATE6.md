# Next-construction request after all harvested route closures

Background was harvested first. The sibling campaign now rigorously proves `L_4<=64`, so the sharp local recurrence/factorial tower fails already at level four, and proves a cyclic-to-Schur ceiling `n<=2S(k)+2`, making symmetric cyclic partitions Schur-hard. Remaining background jobs are unresolved low-value searches in already-banked routes.

Your last advice was implemented: the constant-composition blow-up lemma and exact asymptotic equivalence of separated permutation codes with the full capacity problem are now proved and finite-sanity-checked. This precisely kills permutation *balance as a simplification*, though not arbitrary permutation codes. The fractional bad-clique cover obstruction is also written up with an elementary altered-random-graph witness.

We need growing base or bust. Please now design exactly ONE new concrete correlated family, excluding every banked mechanism in ORACLE_BRIEF.md. It must provide:

1. explicit objects and edge-color / strong-power separation rule;
2. asymptotic parameters that would give `k^{Omega(k)}` vertices with `O(k)` colors, or an effective-capacity/local-seed equivalent;
3. one pivotal lemma plus an actual proof strategy exploiting specific algebraic/combinatorial structure, not a renaming of arbitrary capacity/Ramsey;
4. a smallest discriminating implementation test that can falsify the mechanism quickly.

Promising untested territory would be welcome—for example correlated algebraic/design codes in high-fractional-chromatic triangle-free finite-geometry graphs—but do not force that suggestion if it is unsound. Avoid fixed seeds, iid word sampling, group-difference partitions, first-difference trees, cyclic/Schur constructions, bounded-state automata, bounded-chi_f partial assignments, and unrestricted permutation families. The forbidden recent document and every account of it remain strictly off-limits.