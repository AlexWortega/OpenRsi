# Final growing-base request after GQ-Tanner and polynomial-hitting failures

Background was harvested again: no positive result; optional L4 certification finished, and all live searches are stalled in banked routes.

The GQ-Tanner proposal is now rigorously dead at q=2: balanced configurations are equivalent to six perfect matchings of the Tutte-Coxeter graph with pairwise intersections three; enumeration gives 288 matchings and compatibility clique number exactly 3. Full-collineation quotienting therefore has nothing to quotient at the first parameter, and no q>=8 abundance/rigidity route exists.

Two additional correlated nudges failed:
- GL(d,2) matrix-orbit subsets against nonlinear sum-free Cayley sets gave only bases 1.913 and 2.166 at d=3,4 heuristically.
- zero-constant polynomial evaluation codes would need every nonzero low-degree polynomial to hit a symmetric sum-free set. Exhaustion for p=5,7,11,13 finds avoiding polynomials of degree <=3 for every maximal symmetric sum-free set; natural interval sets already fail to x^2.

With remaining budget, propose exactly ONE different explicit candidate with a genuinely proved core lemma. It must not be another unconstrained “show rigidity/abundance” program. Prefer a construction where the pairwise separation theorem follows from an existing elementary algebraic identity that you can prove fully, leaving at most a quantified counting lemma. Specify the smallest test and a sharp kill criterion. Exclude every route banked in ORACLE_BRIEF.md, including GQ Tanner and polynomial evaluations. If no defensible candidate exists, say so and instead derive the strongest new general obstruction that materially narrows correlated families. Forbidden recent work remains off-limits.