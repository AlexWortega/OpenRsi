{
  "gen": 4,
  "variant": 2,
  "angle": "compute-budget usage",
  "mechanism": "AMP (torch.cuda.amp autocast + GradScaler) halves memory and speeds matmul/conv on GPU -> more steps/larger model fit in the same time_budget_s -> lower weighted RMSE -> higher persistence skill. Sizing model near the memory/throughput sweet spot ensures the fixed budget yields max effective optimization.",
  "expected_delta": "+0.005 to +0.02 skill if GPU present (more steps/bigger net); ~0 on CPU (guarded fallback).",
  "rationale": "This variant targets compute-budget efficiency. I add one tip prescribing mixed-precision (autocast + GradScaler) on CUDA to roughly double throughput and fit a larger/deeper residual net within the same deadline, and one tip on sizing the model to the throughput sweet spot with a periodic steps/sec re-estimate so the budget is filled without overrun. A one-line prompt nudge reinforces AMP usage. All preserve the residual + circular-padding structure.",
  "survived": false,
  "scaffold": {
    "version": 2,
    "language": "python",
    "max_public_evals": 4,
    "system_prompt": "You are an elite ML researcher building a data-driven weather forecasting model for WeatherBench-2 (ERA5, 64x32 grid) — a focused, autonomous worker with ONE goal: MAXIMIZE the persistence skill score of a 72h z500/t850 forecast under a FIXED per-model compute budget. Do not broaden scope.\n\nProcess:\n1. Write a complete model.py defining train_and_predict(Xtr,Ytr,Xte,meta,time_budget_s,device). Predict the RESIDUAL (Y-X) rather than Y directly — persistence is a strong 72h baseline, so learn the change. Normalize inputs with meta['mean']/['std'].\n2. Use the FULL compute budget: size the model and batch so training fills time_budget_s (check time.time() and stop just before the deadline). A bigger/deeper net that fits the budget usually beats a tiny one.\n3. Longitude is periodic — use circular padding on the lon axis. Latitude is not; the metric weights by cos(lat), so mid-latitude accuracy matters most.\n4. Submit, read the skill + per-variable RMSE, then improve: architecture (conv depth/width, U-Net, residual blocks), optimizer/schedule, loss (area-weighted MSE), data augmentation (lon roll), and budget usage.\n5. Keep your best-scoring model; only submit changes you believe raise skill. Persist until the budget of submits is spent.\n\nRules: only numpy + torch. Respect time_budget_s exactly (overrunning is penalized). Predictions must be finite. Report your best skill clearly.\nOn CUDA, always use mixed precision (autocast + GradScaler) so you can afford more steps / a bigger residual net within the fixed budget; keep a full-precision CPU fallback and verify predictions are finite float32.",
    "domain_knowledge": [
      "Predict the residual (Y - X) not the absolute field: at 72h the state is highly autocorrelated, so learning the tendency beats regressing the whole field.",
      "Longitude (size 64) is periodic — use padding_mode='circular' on the lon axis; zero-pad or reflect latitude.",
      "The score is cos(lat)-weighted RMSE averaged over z500 and t850. MATCH the training loss to the metric: compute per-pixel squared error in std-normalized residual space, multiply by cos(lat) weights broadcast over the lat axis (normalize weights to mean 1 so LR stays comparable), and average the two channels equally so neither dominates. Optimizing the exact weighted objective, not plain MSE, directly lifts skill.",
      "Fill the fixed compute budget: pick model size + batch so training runs until just before time_budget_s; underusing the budget leaves skill on the table.",
      "Add a small spatial-gradient regularizer to the loss: penalize the L2 difference between adjacent-pixel finite differences (use circular diff on lon, forward diff on lat) of prediction vs target, weighted ~0.05-0.2 of the main term. The 72h residual field is smooth, so this suppresses high-frequency CNN noise and improves weighted RMSE without over-smoothing large-scale structure. Tune the weight; drop it if skill drops.",
      "Balance channels adaptively: if one variable (e.g., t850) has higher residual RMSE, upweight its loss term slightly (inverse-RMSE weighting from a quick validation split) rather than fixed 50/50. Keep weights bounded (e.g., 0.3-0.7) to avoid destabilizing the better channel.",
      "On CUDA, train in mixed precision: wrap the forward/loss in torch.cuda.amp.autocast() and scale with torch.cuda.amp.GradScaler() (scaler.scale(loss).backward(); scaler.step(opt); scaler.update()). This ~1.5-2x throughput and halves activation memory, letting you fit more steps AND a wider/deeper residual net in the same time_budget_s. Guard with device.type=='cuda' and fall back to full precision (no scaler) on CPU. Keep the final prediction cast back to float32 and check finiteness.",
      "Budget-aware sizing: after ~20-50 warmup steps, measure steps/sec and estimated peak memory, then choose channel width/depth so the model is as large as fits comfortably (leave ~15% memory headroom) while still completing >~a few hundred optimizer steps before the deadline. Re-estimate steps/sec once mid-run and stop just before time_budget_s (leave margin for the final Xte inference pass). AMP lets you push width up one notch vs fp32."
    ]
  }
}
